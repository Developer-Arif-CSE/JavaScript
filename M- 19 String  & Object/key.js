const person={
    name:"arif",
    age:24,
    profession:"developer",
    salary:25000,
    married:true
}

// key diye sudhu property name dekha jay
const keys= Object.keys(person);
console.log(keys);

// values diye sudhu properties er value gula dekha jay
const values=Object.values(person);
console.log(values);